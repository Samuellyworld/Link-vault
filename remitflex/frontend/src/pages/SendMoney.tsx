import Header  from "@/components/Header";
import Send from "@/components/send";

const SendMoney = () => {
  return (
    <>
      <Header />
      <Send/>
    </>
  );
};

export default SendMoney;
